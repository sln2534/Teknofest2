import 'package:flutter/widgets.dart';

class LiveSupportView extends StatelessWidget {
  const LiveSupportView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Canlı Destek'));
  }
}
